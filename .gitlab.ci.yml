stages:
  - test

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

test:
  stage: test
  image: maven:3.9.9-eclipse-temurin-25
  cache:
    paths:
      - .m2/repository
  script:
    - mvn -version
    - mvn -f blackdrongo-selenium/pom.xml test
  artifacts:
    when: always
    paths:
      - blackdrongo-selenium/target/surefire-reports/
      - blackdrongo-selenium/target/cucumber-reports/
